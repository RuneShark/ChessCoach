# pipeline
python-chess>=1.999
requests>=2.28

# web UI (board + engine + coach chat)
fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
httpx>=0.27      # direct call to a local Ollama backend (COACH_BACKEND=local)
